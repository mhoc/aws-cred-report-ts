{
  "name": "@mhoc/aws-cred-report",
  "version": "0.4.0",
  "license": "MIT",
  "description": "helper library for generating aws credential reports",
  "main": "dist/index",
  "scripts": {
    "build": "./node_modules/.bin/tsc",
    "build-docs": "./node_modules/.bin/typedoc --out docs --mode file",
    "docs": "npm run build-docs && cd docs && python -m SimpleHTTPServer"
  },
  "dependencies": {
    "aws-sdk": "^2.761.0",
    "csv-parse": "^4.12.0"
  },
  "devDependencies": {
    "@types/node": "^12.12.62",
    "typedoc": "^0.19.2",
    "typescript": "^4.0.3"
  }
}
