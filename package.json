{
  "name": "@mhoc/aws-cred-report",
  "version": "0.5.0",
  "license": "MIT",
  "description": "helper library for generating aws credential reports",
  "main": "dist/index",
  "scripts": {
    "build": "./node_modules/.bin/tsc",
    "docs:build": "./node_modules/.bin/typedoc --theme minimal ./src/index.ts",
    "docs:host": "cd docs && python -m SimpleHTTPServer"
  },
  "dependencies": {
    "aws-sdk": "^2.850.0",
    "csv-parse": "^4.15.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "typedoc": "^0.20.28",
    "typescript": "^4.2.2"
  }
}
